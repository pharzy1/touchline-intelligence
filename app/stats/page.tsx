export default async function StatsPage() {
  return <main><section className="page-hero shell"><p className="eyebrow">PRODUCTION TELEMETRY</p><h1>System stats</h1><p>Touchline records route volume, status, and latency in D1. Events are intentionally anonymous: no names, emails, or IP addresses are persisted.</p></section><section className="shell" style={{ paddingBottom: "5rem" }}><div className="reference-card"><div><span>INSTRUMENTATION</span><h2>Structured edge telemetry</h2><p>Every model request emits searchable JSON logs and a fail-open D1 event. Rate limits protect the public endpoints per edge isolate.</p></div><strong>LIVE</strong></div><p className="scout-disclaimer">Query <code>/api/stats</code> for machine-readable aggregate counts and average latency.</p></section></main>;
}
